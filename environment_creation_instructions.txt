pip:

1) Create a new virtual environment: virtualenv name
2) activate the new virtual environment: source name/bin/activate
3) install the needed libraries: pip install -r requirements.txt

conda:

Create a new virtual environment with the needed libraries: conda create --name <env_name> --file requirements.txt
activate the environment: activate <env_name>
